apiVersion: 1
providers:
  - name: "ecotwin-dashboards"
    folder: "EcoTwin"
    type: file
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
